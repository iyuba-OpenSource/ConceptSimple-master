<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <import type="android.view.View"/>
        <variable
            name="item"
            type="com.suzhou.concept.bean.SettingItem" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="15dp"
        android:paddingBottom="15dp"
        android:paddingStart="10dp"
        android:paddingEnd="10dp">

        <ImageView
            android:id="@+id/setting_item_icon"
            loadDrawable="@{item.icon}"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:contentDescription="@string/todo" />
        <TextView
            android:layout_toEndOf="@id/setting_item_icon"
            android:textSize="@dimen/standard_text"
            android:layout_marginStart="10dp"
            android:text="@{item.item.type}"
            android:gravity="center"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:ignore="RelativeOverlap" />


        <ImageView
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:layout_toStartOf="@id/setting_item_some"
            android:visibility="@{ item.cache ? View.INVISIBLE :View.VISIBLE}"
            android:src="@drawable/right"
            android:layout_width="15sp"
            android:layout_height="15sp"
            android:contentDescription="@string/todo" />
        <TextView
            android:id="@+id/setting_item_some"
            android:layout_alignParentEnd="true"
            android:visibility="@{ item.cache ?  View.VISIBLE:View.INVISIBLE}"
            android:text="@{item.value}"
            android:gravity="center"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
    </RelativeLayout>
</layout>